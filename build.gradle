apply plugin: 'java'

repositories {
    mavenCentral()
    flatDir dirs: 'lib/test'
}

dependencies {
    compile 'org.testng:testng:6+'
    compile 'org.easytesting:fest-assert-core:2.0+'
    compile 'junit:junit:4+'

    testCompile 'org.mockito:mockito-all:1+'
    testCompile name: 'mockito-testng-1.0'
}

test {
    useTestNG()

    exclude 'test/**'
}
